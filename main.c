#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "fa.h"
#include "construct.h"

int main() {

  // FA new = new_FA(2, "ab");
  // set_transition(new, 0, 'a', 0);
  // set_transition(new, 0, 'b', 1);
  // set_transition(new, 1, 'a', 1);
  // set_transition(new, 1, 'b', 0);
  // set_accepting(new, 1);
  //
  // execute(new, "abbab");

  foo("a|b");


}
